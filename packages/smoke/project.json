{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",

  "targets": {
    "install": {
      "command": "pnpm i",
      "dependsOn": ["^build"],
      "inputs": ["^go-files", "^ts-files"],
      "outputs": ["{projectRoot}/node_modules"],
      "cache": true,
      "options": {
        "cwd": "{projectRoot}"
      }
    },

    "verify": {
      "dependsOn": ["install"],
      "command": "pnpm run smoke",
      "options": {
        "cwd": "{projectRoot}"
      }
    }
  }
}
